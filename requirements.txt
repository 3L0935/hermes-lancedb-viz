# The retrieval gate (bm25 threshold / abstention) is calibrated against the
# score produced by a SPECIFIC lancedb version: identical code and identical
# rows score differently across versions. Pinned here so the container and the
# host venv cannot drift apart and silently invalidate the calibration.
lancedb==0.34.0
numpy>=1.26.0
pyarrow>=14.0.0
pyyaml>=6.0
httpx>=0.27.0
scikit-learn>=1.4.0
# Embedding projection (/api/projection, the Embeddings page). Optional at
# runtime — the endpoint reports `dependency_missing` and the UI says so — but
# declared here so a container rebuild does not silently remove a feature the
# README advertises.
umap-learn>=0.5,<0.6
